---
phase: 02-artifact-lifecycle-storage
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - packages/lattice/src/storage/storage.ts
  - packages/lattice/src/storage/fingerprint.ts
  - packages/lattice/src/storage/memory.ts
  - packages/lattice/src/storage/local.ts
  - packages/lattice/test/artifact-storage.test.ts
  - packages/lattice/test/artifact-local-store.test.ts
  - packages/lattice/test/runtime-config.test.ts
autonomous: true
requirements: [ART-02, ART-04]
requirements_addressed: [ART-02, ART-04]
must_haves:
  truths:
    - "Developer can store artifacts in an in-memory store and retrieve either metadata-only refs or loaded payload-bearing artifacts."
    - "Developer can store artifacts in an inspectable local filesystem fixture layout with metadata envelopes separate from payload files."
    - "Storage refs point back to the concrete store and key, and stored artifacts receive SHA-256 fingerprints when the payload is available."
  artifacts:
    - path: "packages/lattice/src/storage/storage.ts"
      provides: "ArtifactStore, StorageLike, and local fixture envelope contracts"
      contains: "interface ArtifactStore"
    - path: "packages/lattice/src/storage/memory.ts"
      provides: "createMemoryArtifactStore development store"
      contains: "createMemoryArtifactStore"
    - path: "packages/lattice/src/storage/local.ts"
      provides: "createLocalArtifactStore inspectable filesystem fixture store"
      contains: "metadata.json"
    - path: "packages/lattice/src/storage/fingerprint.ts"
      provides: "SHA-256 fingerprint helpers for available payloads"
      contains: "sha256"
    - path: "packages/lattice/test/artifact-storage.test.ts"
      provides: "Memory store behavior coverage"
    - path: "packages/lattice/test/artifact-local-store.test.ts"
      provides: "Filesystem fixture layout and reload coverage"
  key_links:
    - from: "packages/lattice/src/storage/memory.ts"
      to: "packages/lattice/src/artifacts/artifact.ts"
      via: "stored refs are created with toArtifactRef and include storage refs"
      pattern: "toArtifactRef"
    - from: "packages/lattice/src/storage/local.ts"
      to: "packages/lattice/src/storage/storage.ts"
      via: "metadata envelope format separates ref metadata from payload loading"
      pattern: "StoredArtifactEnvelope"
    - from: "packages/lattice/src/runtime/config.ts"
      to: "packages/lattice/src/storage/storage.ts"
      via: "LatticeConfig storage continues to use provider-neutral StorageLike"
      pattern: "StorageLike"
---

<objective>
Add development artifact stores for memory and local filesystem fixtures.

Purpose: Phase 2 requires storage that can persist and reload artifacts for development, tests, and later replay fixtures while keeping metadata/ref access separate from raw payload loading.
Output: A provider-neutral `ArtifactStore` contract, in-memory store, local filesystem store, SHA-256 fingerprinting for available payloads, and runtime tests for store behavior.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-artifact-lifecycle-storage/02-CONTEXT.md
@.planning/phases/02-artifact-lifecycle-storage/02-RESEARCH.md
@.planning/phases/02-artifact-lifecycle-storage/02-01-SUMMARY.md
@packages/lattice/src/artifacts/artifact.ts
@packages/lattice/src/storage/storage.ts
@packages/lattice/src/runtime/config.ts
@packages/lattice/test/runtime-config.test.ts

<interfaces>
Required artifact contracts from Plan 01:

```typescript
export interface ArtifactStorageRef {
  readonly storeId: string;
  readonly key: string;
}

export interface ArtifactFingerprint {
  readonly algorithm: "sha256";
  readonly value: string;
}

export interface ArtifactRef {
  readonly id: string;
  readonly kind: ArtifactKind;
  readonly source: ArtifactSource;
  readonly privacy: ArtifactPrivacy;
  readonly storage?: ArtifactStorageRef;
  readonly fingerprint?: ArtifactFingerprint;
}

export type ArtifactInput = ArtifactRef & {
  readonly value?: unknown;
};

export function toArtifactRef(artifact: ArtifactInput | ArtifactRef): ArtifactRef;
```

Locked decisions implemented here: D-08 and D-13 through D-17. Deferred items that must not be implemented: S3, SQLite, Postgres, remote object stores, multi-tenant production stores, provider uploads, provider packaging, and replay envelopes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define ArtifactStore and implement memory storage</name>
  <read_first>
    .planning/phases/02-artifact-lifecycle-storage/02-CONTEXT.md
    .planning/phases/02-artifact-lifecycle-storage/02-RESEARCH.md
    .planning/phases/02-artifact-lifecycle-storage/02-01-SUMMARY.md
    packages/lattice/src/artifacts/artifact.ts
    packages/lattice/src/storage/storage.ts
    packages/lattice/src/runtime/config.ts
    packages/lattice/test/runtime-config.test.ts
  </read_first>
  <files>packages/lattice/src/storage/storage.ts, packages/lattice/src/storage/fingerprint.ts, packages/lattice/src/storage/memory.ts, packages/lattice/test/artifact-storage.test.ts, packages/lattice/test/runtime-config.test.ts</files>
  <behavior>
    - `createMemoryArtifactStore({ id: "mem-test" }).put(artifact.text("hello"))` returns an `ArtifactRef` with `storage: { storeId: "mem-test", key: <artifact id> }`, no `value`, and `fingerprint.algorithm === "sha256"`.
    - `get(id)` returns metadata/ref only and has no `value` property.
    - `load(id)` returns the payload-bearing `ArtifactInput` and preserves `value`.
    - `has(id)`, `delete(id)`, and `list()` reflect store state.
    - `normalizeConfig({ storage: createMemoryArtifactStore() })` preserves the concrete store object.
  </behavior>
  <action>
    Replace the Phase 1 placeholder `StorageLike` in `packages/lattice/src/storage/storage.ts` with a provider-neutral artifact storage contract per D-14 and D-17:

    ```typescript
    export interface ArtifactStore {
      readonly kind: "artifact-store";
      readonly id: string;
      put(artifact: ArtifactInput): Promise<ArtifactRef>;
      get(id: string): Promise<ArtifactRef | undefined>;
      load(id: string): Promise<ArtifactInput | undefined>;
      has(id: string): Promise<boolean>;
      delete(id: string): Promise<boolean>;
      list(): Promise<readonly ArtifactRef[]>;
    }

    export type StorageLike = ArtifactStore;
    ```

    Add `StoredArtifactEnvelope` with exact fields `version: 1`, `ref: ArtifactRef`, and optional `payload` descriptor so local storage can use the same envelope shape.

    Create `packages/lattice/src/storage/fingerprint.ts` with `fingerprintArtifactValue(value)` that returns `{ algorithm: "sha256", value }` for available string, JSON-serializable object, Blob/File, ArrayBuffer, and Uint8Array payloads. Use standard SHA-256 and do not use fingerprints as public IDs per D-08.

    Create `packages/lattice/src/storage/memory.ts` with `createMemoryArtifactStore(options?: { readonly id?: string }): ArtifactStore`. Default id must be `"memory"`. Store cloned refs and payloads in memory only. `put` must compute a fingerprint when absent and possible, attach `storage`, and return `toArtifactRef(storedArtifact)`. `get` and `list` must return payload-free refs. `load` may return the stored payload-bearing artifact. Update `runtime-config.test.ts` so the storage fixture uses `createMemoryArtifactStore({ id: "config-store" })` instead of the Phase 1 `{ kind: "storage", put }` placeholder.
  </action>
  <verify>
    <automated>pnpm --filter lattice test -- artifact-storage.test.ts runtime-config.test.ts && pnpm --filter lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'interface ArtifactStore|export type StorageLike = ArtifactStore' packages/lattice/src/storage/storage.ts` finds the storage contract.
    - `rg 'createMemoryArtifactStore|kind: "artifact-store"|id: options\\?\\.id \\?\\? "memory"' packages/lattice/src/storage/memory.ts` finds the memory store factory and default id.
    - `rg 'sha256|fingerprintArtifactValue' packages/lattice/src/storage/fingerprint.ts packages/lattice/test/artifact-storage.test.ts` finds fingerprint implementation and test coverage.
    - `rg 'createMemoryArtifactStore\\(\\{ id: "config-store" \\}\\)' packages/lattice/test/runtime-config.test.ts` finds the updated config fixture.
    - `pnpm --filter lattice test -- artifact-storage.test.ts runtime-config.test.ts` passes.
  </acceptance_criteria>
  <done>Memory storage supports put/get/load/has/delete/list with metadata-only refs, payload loading separation, storage refs, and SHA-256 fingerprints for available payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement inspectable local filesystem artifact storage</name>
  <read_first>
    .planning/phases/02-artifact-lifecycle-storage/02-CONTEXT.md
    .planning/phases/02-artifact-lifecycle-storage/02-RESEARCH.md
    .planning/phases/02-artifact-lifecycle-storage/02-01-SUMMARY.md
    packages/lattice/src/artifacts/artifact.ts
    packages/lattice/src/storage/storage.ts
    packages/lattice/src/storage/fingerprint.ts
    packages/lattice/src/storage/memory.ts
  </read_first>
  <files>packages/lattice/src/storage/local.ts, packages/lattice/test/artifact-local-store.test.ts</files>
  <behavior>
    - `createLocalArtifactStore(tempDir, { id: "local-test" }).put(artifact.json({ ok: true }))` creates `<tempDir>/artifacts/<encoded-id>/metadata.json` and a separate payload file.
    - `metadata.json` is readable JSON with `version: 1`, a payload-free `ref`, and `payload.path` pointing at the separate payload file.
    - `get(id)` reads only metadata and returns no `value`; `load(id)` reads the payload file and returns the original value.
    - `list()` returns refs sorted by id for deterministic fixture output.
    - `delete(id)` removes the artifact directory and returns true for an existing artifact.
  </behavior>
  <action>
    Create `packages/lattice/src/storage/local.ts` with `createLocalArtifactStore(rootDir: string | URL, options?: { readonly id?: string }): ArtifactStore`. Default id must be `"local"`. Use Node built-ins only: `node:fs/promises`, `node:path`, and `node:url` as needed. Keep Node-specific imports inside this local store module per D-17.

    Use this exact fixture layout per D-15:

    ```text
    <root>/artifacts/<encodeURIComponent(artifact.id)>/metadata.json
    <root>/artifacts/<encodeURIComponent(artifact.id)>/payload.json
    ```

    `metadata.json` must contain a `StoredArtifactEnvelope` with `version: 1`, payload-free `ref`, and `payload: { kind: "json", path: "payload.json" }` for JSON-serializable values. For Blob/File/ArrayBuffer/Uint8Array values, store bytes in `payload.bin` and use `payload: { kind: "binary", path: "payload.bin" }`. For artifacts without value, omit `payload`.

    `put` must not read string paths from `artifact.file("path")`; it should persist the artifact value as metadata/payload only. `get` must read `metadata.json` only. `load` must read payload only when the envelope has a payload descriptor. `list` must scan `artifacts/*/metadata.json` and sort refs by `id`. `delete` must remove the encoded artifact directory recursively. Do not implement replay envelopes, SQLite, Postgres, S3, provider uploads, or production durability per D-13 and deferred scope.
  </action>
  <verify>
    <automated>pnpm --filter lattice test -- artifact-local-store.test.ts && pnpm --filter lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'createLocalArtifactStore|metadata\\.json|payload\\.json|payload\\.bin' packages/lattice/src/storage/local.ts packages/lattice/test/artifact-local-store.test.ts` finds the local store factory and fixture layout.
    - `rg 'get\\(id: string\\)|load\\(id: string\\)' packages/lattice/src/storage/local.ts` finds separate metadata and payload loading methods.
    - `rg 'rm\\(.*recursive: true' packages/lattice/src/storage/local.ts` finds directory deletion behavior.
    - `! rg 'sqlite|postgres|s3|aws|upload|replay envelope' packages/lattice/src/storage packages/lattice/test/artifact-local-store.test.ts` returns no matches.
    - `pnpm --filter lattice test -- artifact-local-store.test.ts` passes.
  </acceptance_criteria>
  <done>Local filesystem storage writes inspectable metadata envelopes and separate payload files, with get/load separation and full development-store operations.</done>
</task>

</tasks>

<verification>
Run the plan-level checks after both tasks:

```bash
pnpm --filter lattice test -- artifact-storage.test.ts artifact-local-store.test.ts runtime-config.test.ts
pnpm --filter lattice typecheck
```
</verification>

<success_criteria>
ART-04 is satisfied because memory and local filesystem stores can persist, list, delete, metadata-get, and payload-load artifacts. ART-02 storage refs and fingerprints are attached during storage without changing public artifact ids.
</success_criteria>

<output>
After completion, create `.planning/phases/02-artifact-lifecycle-storage/02-02-SUMMARY.md`.
</output>
