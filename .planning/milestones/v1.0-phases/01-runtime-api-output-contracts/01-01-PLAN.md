---
phase: 01-runtime-api-output-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - packages/lattice/package.json
  - packages/lattice/tsconfig.json
  - packages/lattice/tsdown.config.ts
  - packages/lattice/vitest.config.ts
  - packages/lattice/src/index.ts
  - packages/lattice/test/scaffold.test.ts
autonomous: true
requirements:
  - API-01
must_haves:
  truths:
    - "Developer can import from the local workspace package name `lattice`."
    - "The package is ESM-first and targets Node >=24."
    - "The package has repeatable build, typecheck, runtime test, type-test, and package-lint scripts."
  artifacts:
    - path: "package.json"
      provides: "Root pnpm workspace scripts and dev dependency versions"
      contains: "\"packageManager\": \"pnpm@10.33.1\""
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package discovery and dependency catalog"
      contains: "packages/*"
    - path: "packages/lattice/package.json"
      provides: "Local package named lattice with explicit ESM export map"
      contains: "\"name\": \"lattice\""
    - path: "packages/lattice/src/index.ts"
      provides: "Initial public entrypoint"
      contains: "export const latticeVersion"
  key_links:
    - from: "packages/lattice/package.json"
      to: "packages/lattice/src/index.ts"
      via: "exports import target built by tsdown"
      pattern: "\"import\": \"./dist/index.js\""
    - from: "packages/lattice/tsdown.config.ts"
      to: "packages/lattice/src/index.ts"
      via: "entry configuration"
      pattern: "entry: \\[\"src/index.ts\"\\]"
---

<objective>
Create the initial TypeScript-first package scaffold for the local `lattice` workspace package.

Purpose: Satisfy API-01 by giving executors a concrete Node >=24, pnpm, TypeScript 6, ESM-first package foundation before public API contracts are implemented.
Output: Root workspace metadata, package metadata, TypeScript/build/test config, a temporary public entrypoint, and a scaffold smoke test.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-runtime-api-output-contracts/01-CONTEXT.md
@.planning/phases/01-runtime-api-output-contracts/01-RESEARCH.md
@AGENTS.md

<interfaces>
Greenfield repo: no source package exists before this plan. This plan creates the package boundary later plans depend on.

Locked decisions implemented here:
- D-01: Primary import style is named exports from `"lattice"`.

Research constraints implemented here:
- Node target: `>=24`.
- TypeScript version: `6.0.3`.
- pnpm workspace/catalog package manager.
- ESM-first package with explicit `exports`.
- Build: `tsdown`.
- Tests: `vitest`.
- Package checks: `publint` and `@arethetypeswrong/cli`.
- Local package name stays `lattice`; npm name conflict remains a future publishing decision, not a Phase 1 blocker.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workspace and package metadata</name>
  <files>package.json, pnpm-workspace.yaml, packages/lattice/package.json</files>
  <read_first>
    - package.json
    - pnpm-workspace.yaml
    - packages/lattice/package.json
    - .planning/phases/01-runtime-api-output-contracts/01-CONTEXT.md
    - .planning/phases/01-runtime-api-output-contracts/01-RESEARCH.md
    - AGENTS.md
  </read_first>
  <action>
    Create missing directories and metadata for a pnpm workspace.

    Root `package.json` must use these concrete values:
    - `"name": "lattice-workspace"`
    - `"private": true`
    - `"type": "module"`
    - `"packageManager": "pnpm@10.33.1"`
    - `"engines": { "node": ">=24" }`
    - scripts: `"build": "pnpm -r build"`, `"typecheck": "pnpm -r typecheck"`, `"test": "pnpm -r test"`, `"test:types": "pnpm -r test:types"`, `"lint:packages": "pnpm -r lint:packages"`, `"changeset": "changeset"`
    - devDependencies: `@arethetypeswrong/cli` `0.18.2`, `@changesets/cli` `2.31.0`, `@vitest/coverage-v8` `4.1.5`, `publint` `0.3.18`, `tsd` `0.33.0`, `tsdown` `0.21.9`, `typescript` `6.0.3`, `vitest` `4.1.5`

    `pnpm-workspace.yaml` must use:
    - `packages: ["packages/*"]`
    - catalog entries: `@standard-schema/spec: 1.1.0`, `zod: 4.3.6`, `typescript: 6.0.3`, `tsdown: 0.21.9`, `vitest: 4.1.5`, `tsd: 0.33.0`, `publint: 0.3.18`, `@arethetypeswrong/cli: 0.18.2`

    `packages/lattice/package.json` must use these concrete values:
    - `"name": "lattice"` per D-01 and research package-name guidance
    - `"version": "0.0.0"`
    - `"description": "TypeScript-first capability runtime SDK for AI applications"`
    - `"type": "module"`
    - `"sideEffects": false`
    - `"engines": { "node": ">=24" }`
    - `"exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } }`
    - `"types": "./dist/index.d.ts"`
    - `"files": ["dist"]`
    - scripts: `"build": "tsdown"`, `"typecheck": "tsc -p tsconfig.json --noEmit"`, `"test": "vitest run"`, `"test:types": "vitest typecheck --run && tsd"`, `"lint:packages": "pnpm build && publint && attw --pack . --profile esm-only"`
    - dependencies: `"@standard-schema/spec": "catalog:"`
    - devDependencies: `"zod": "catalog:"`

    Do not add provider SDK dependencies such as `openai`, `ai`, `@ai-sdk/provider`, Anthropic, or Gemini packages in Phase 1.
  </action>
  <verify>
    <automated>test -f package.json && test -f pnpm-workspace.yaml && test -f packages/lattice/package.json && rg '"packageManager": "pnpm@10\\.33\\.1"|"name": "lattice"|"node": ">=24"|"@standard-schema/spec": "catalog:"' package.json pnpm-workspace.yaml packages/lattice/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"packageManager": "pnpm@10.33.1"`.
    - `package.json` contains `"build": "pnpm -r build"`.
    - `pnpm-workspace.yaml` contains `packages/*`.
    - `pnpm-workspace.yaml` contains `typescript: 6.0.3`.
    - `packages/lattice/package.json` contains `"name": "lattice"`.
    - `packages/lattice/package.json` contains `"import": "./dist/index.js"`.
    - `packages/lattice/package.json` contains `"@standard-schema/spec": "catalog:"`.
    - `rg 'openai|@ai-sdk|anthropic|gemini' package.json packages/lattice/package.json` returns no matches.
  </acceptance_criteria>
  <done>Workspace metadata exists and the `lattice` package has an explicit ESM export map with no provider SDK dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Create TypeScript, build, and test scaffold</name>
  <files>tsconfig.base.json, packages/lattice/tsconfig.json, packages/lattice/tsdown.config.ts, packages/lattice/vitest.config.ts, packages/lattice/src/index.ts, packages/lattice/test/scaffold.test.ts</files>
  <read_first>
    - tsconfig.base.json
    - packages/lattice/tsconfig.json
    - packages/lattice/tsdown.config.ts
    - packages/lattice/vitest.config.ts
    - packages/lattice/src/index.ts
    - packages/lattice/test/scaffold.test.ts
    - packages/lattice/package.json
    - .planning/phases/01-runtime-api-output-contracts/01-RESEARCH.md
  </read_first>
  <action>
    Create the initial source/test directories and config files.

    `tsconfig.base.json` must include:
    - `"target": "ES2024"`
    - `"module": "ESNext"`
    - `"moduleResolution": "Bundler"`
    - `"lib": ["ES2024", "DOM", "DOM.Iterable"]`
    - `"strict": true`
    - `"exactOptionalPropertyTypes": true`
    - `"noUncheckedIndexedAccess": true`
    - `"verbatimModuleSyntax": true`
    - `"declaration": true`
    - `"declarationMap": true`
    - `"sourceMap": true`
    - `"skipLibCheck": false`

    `packages/lattice/tsconfig.json` must extend `../../tsconfig.base.json`, omit `rootDir` entirely so package typecheck can include source, runtime tests, and type tests without TS6059, set `"outDir": "dist"`, and include `["src", "test", "test-d"]`.

    `packages/lattice/tsdown.config.ts` must export `defineConfig({ entry: ["src/index.ts"], format: ["esm"], dts: true, sourcemap: true, clean: true, treeshake: true })`.

    `packages/lattice/vitest.config.ts` must use `defineConfig` from `vitest/config` and include `test: { environment: "node", typecheck: { tsconfig: "./tsconfig.json" } }`.

    `packages/lattice/src/index.ts` is a temporary scaffold entrypoint and must export:
    - `export const latticeVersion = "0.0.0";`

    `packages/lattice/test/scaffold.test.ts` must import `{ latticeVersion }` from `../src/index.js` and assert it equals `"0.0.0"`.
  </action>
  <verify>
    <automated>pnpm install && pnpm --filter lattice typecheck && pnpm --filter lattice test</automated>
  </verify>
  <acceptance_criteria>
    - `tsconfig.base.json` contains `"moduleResolution": "Bundler"`.
    - `tsconfig.base.json` contains `"exactOptionalPropertyTypes": true`.
    - `tsconfig.base.json` contains `"noUncheckedIndexedAccess": true`.
    - `packages/lattice/tsconfig.json` contains `"outDir": "dist"`.
    - `packages/lattice/tsconfig.json` does not contain `"rootDir"`.
    - `packages/lattice/tsdown.config.ts` contains `entry: ["src/index.ts"]`.
    - `packages/lattice/vitest.config.ts` contains `environment: "node"`.
    - `packages/lattice/src/index.ts` contains `export const latticeVersion = "0.0.0";`.
    - `packages/lattice/test/scaffold.test.ts` contains `expect(latticeVersion).toBe("0.0.0")`.
    - `pnpm --filter lattice typecheck` exits 0.
    - `pnpm --filter lattice test` exits 0.
  </acceptance_criteria>
  <done>TypeScript, tsdown, and Vitest are configured and the local package has a passing scaffold test.</done>
</task>

</tasks>

<verification>
Run `pnpm --filter lattice typecheck`, `pnpm --filter lattice test`, and `pnpm --filter lattice build`. Confirm generated declarations resolve from `packages/lattice/dist/index.d.ts`.
</verification>

<success_criteria>
Plan 01 is complete when the repo has a local pnpm workspace package named `lattice`, Node >=24 and TypeScript 6 are enforced in config, the package builds as ESM through tsdown, and initial typecheck/test commands pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-api-output-contracts/01-01-SUMMARY.md`.
</output>
