---
phase: 04-context-sessions-provider-packaging
plan: 01
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - packages/lattice/src/sessions/session.ts
  - packages/lattice/src/context/context-pack.ts
  - packages/lattice/src/providers/packaging.ts
  - packages/lattice/src/providers/adapters.ts
  - packages/lattice/src/runtime/config.ts
  - packages/lattice/src/runtime/create-ai.ts
  - packages/lattice/test/context-provider-replay-tools.test.ts
autonomous: true
requirements: [API-05, CTX-01, CTX-02, CTX-03, CTX-04, CTX-05, CTX-06, CTX-07, PROV-01, PROV-02, PROV-03, PROV-04, PROV-05, PROV-06]
requirements_addressed: [API-05, CTX-01, CTX-02, CTX-03, CTX-04, CTX-05, CTX-06, CTX-07, PROV-01, PROV-02, PROV-03, PROV-04, PROV-05, PROV-06]
must_haves:
  truths:
    - "Runtime supports explicit sessions, branches, persisted turns, artifacts, summaries, and plan history."
    - "Context packs record included, summarized, archived, and omitted items with reasons and trust labels."
    - "Provider packaging chooses policy-safe transports and records provider-packaging lineage."
    - "OpenAI, AI SDK, and OpenAI-compatible adapters are exposed through narrow Lattice-owned factories."
---

<objective>
Implement session, context, provider packaging, and narrow adapter surfaces.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Sessions, context packs, packaging, and adapter factories</name>
  <read_first>
    packages/lattice/src/runtime/config.ts
    packages/lattice/src/runtime/create-ai.ts
    packages/lattice/src/providers/provider.ts
  </read_first>
  <action>
    Add memory session store, context pack builder, runtime overrides, provider packaging, and adapter factories for OpenAI, AI SDK, and OpenAI-compatible HTTP.
  </action>
  <acceptance_criteria>
    - `packages/lattice/src/sessions/session.ts` contains `createMemorySessionStore`.
    - `packages/lattice/src/context/context-pack.ts` contains `buildContextPack`.
    - `packages/lattice/src/providers/packaging.ts` contains `packageArtifactsForProvider`.
    - `packages/lattice/src/providers/adapters.ts` contains `createOpenAICompatibleProvider`, `createOpenAIProvider`, and `createAISdkProvider`.
  </acceptance_criteria>
</task>
</tasks>

<verification>
`pnpm --filter lattice test -- context-provider-replay-tools.test.ts runtime.test.ts`
</verification>
