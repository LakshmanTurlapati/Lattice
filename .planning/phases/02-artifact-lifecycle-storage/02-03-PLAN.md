---
phase: 02-artifact-lifecycle-storage
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - packages/lattice/src/providers/provider.ts
  - packages/lattice/src/runtime/create-ai.ts
  - packages/lattice/src/outputs/validate.ts
  - packages/lattice/src/runtime/public-types.ts
  - packages/lattice/src/index.ts
  - packages/lattice/test/outputs.test.ts
  - packages/lattice/test/runtime.test.ts
  - packages/lattice/test/public-api.test-d.ts
  - packages/lattice/test-d/package-types.test-d.ts
autonomous: true
requirements: [ART-01, ART-02, ART-03, ART-04, ART-05]
requirements_addressed: [ART-01, ART-02, ART-03, ART-04, ART-05]
must_haves:
  truths:
    - "Developer can import artifact helpers, artifact/store types, createMemoryArtifactStore, and createLocalArtifactStore from the public lattice entrypoint."
    - "Provider adapter artifact refs and output.artifacts() values are typed and normalized as payload-free ArtifactRef objects."
    - "Generated outputs, tool results, transcripts, and provider-handle placeholders can be represented as artifacts with lineage rather than ad hoc result fields."
  artifacts:
    - path: "packages/lattice/src/index.ts"
      provides: "Named public exports for artifact namespace, store factories, and public types"
      contains: "createMemoryArtifactStore"
    - path: "packages/lattice/src/runtime/public-types.ts"
      provides: "Public type barrel for artifact lifecycle and storage contracts"
      contains: "ArtifactStore"
    - path: "packages/lattice/src/providers/provider.ts"
      provides: "Provider-neutral adapter request/response types using ArtifactInput and ArtifactRef"
      contains: "artifactRefs"
    - path: "packages/lattice/src/runtime/create-ai.ts"
      provides: "Runtime sanitization of adapter artifact refs"
      contains: "toArtifactRef"
    - path: "packages/lattice/src/outputs/validate.ts"
      provides: "Validation and payload stripping for output.artifacts()"
      contains: "Expected artifacts output"
    - path: "packages/lattice/test-d/package-types.test-d.ts"
      provides: "Package-boundary type coverage for public artifact/storage APIs"
  key_links:
    - from: "packages/lattice/src/index.ts"
      to: "packages/lattice/src/storage/memory.ts"
      via: "named public value export"
      pattern: "export \\{ createMemoryArtifactStore \\}"
    - from: "packages/lattice/src/runtime/create-ai.ts"
      to: "packages/lattice/src/artifacts/artifact.ts"
      via: "adapter artifact refs are mapped through toArtifactRef before returning RunSuccess.artifacts"
      pattern: "toArtifactRef"
    - from: "packages/lattice/src/outputs/validate.ts"
      to: "packages/lattice/src/artifacts/artifact.ts"
      via: "output.artifacts() validates ArtifactRef shape and strips payloads"
      pattern: "isArtifactRef|toArtifactRef"
---

<objective>
Wire the artifact lifecycle into the public package and runtime/output boundaries.

Purpose: Phase 2 is not complete until developers can use the new artifact/store APIs through `lattice`, and generated/tool/transcript/provider-handle results flow as artifact refs instead of raw ad hoc fields.
Output: Public exports, provider-neutral artifact ref typing, runtime ref sanitization, output artifact validation, and source/package type tests.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-artifact-lifecycle-storage/02-CONTEXT.md
@.planning/phases/02-artifact-lifecycle-storage/02-RESEARCH.md
@.planning/phases/02-artifact-lifecycle-storage/02-01-SUMMARY.md
@.planning/phases/02-artifact-lifecycle-storage/02-02-SUMMARY.md
@packages/lattice/src/artifacts/artifact.ts
@packages/lattice/src/storage/storage.ts
@packages/lattice/src/storage/memory.ts
@packages/lattice/src/storage/local.ts
@packages/lattice/src/providers/provider.ts
@packages/lattice/src/runtime/create-ai.ts
@packages/lattice/src/outputs/validate.ts
@packages/lattice/src/runtime/public-types.ts
@packages/lattice/src/index.ts
@packages/lattice/test/public-api.test-d.ts
@packages/lattice/test-d/package-types.test-d.ts

<interfaces>
Required contracts from Plans 01 and 02:

```typescript
export type ArtifactInput = ArtifactRef & {
  readonly value?: unknown;
};

export function toArtifactRef(artifact: ArtifactInput | ArtifactRef): ArtifactRef;
export function isArtifactRef(value: unknown): value is ArtifactRef;

export interface ArtifactStore {
  readonly kind: "artifact-store";
  readonly id: string;
  put(artifact: ArtifactInput): Promise<ArtifactRef>;
  get(id: string): Promise<ArtifactRef | undefined>;
  load(id: string): Promise<ArtifactInput | undefined>;
  has(id: string): Promise<boolean>;
  delete(id: string): Promise<boolean>;
  list(): Promise<readonly ArtifactRef[]>;
}
```

Locked decisions implemented here: D-01, D-02, D-06, D-10, D-16, and D-18 through D-21. This plan must not add real provider packaging, uploads, tool execution, MCP, context packs, routing, or replay envelopes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize provider and output artifact refs at runtime boundaries</name>
  <read_first>
    .planning/phases/02-artifact-lifecycle-storage/02-CONTEXT.md
    .planning/phases/02-artifact-lifecycle-storage/02-RESEARCH.md
    .planning/phases/02-artifact-lifecycle-storage/02-01-SUMMARY.md
    packages/lattice/src/artifacts/artifact.ts
    packages/lattice/src/providers/provider.ts
    packages/lattice/src/runtime/create-ai.ts
    packages/lattice/src/outputs/contracts.ts
    packages/lattice/src/outputs/validate.ts
    packages/lattice/test/outputs.test.ts
    packages/lattice/test/runtime.test.ts
  </read_first>
  <files>packages/lattice/src/providers/provider.ts, packages/lattice/src/runtime/create-ai.ts, packages/lattice/src/outputs/validate.ts, packages/lattice/test/outputs.test.ts, packages/lattice/test/runtime.test.ts</files>
  <behavior>
    - `ProviderRunRequest.artifacts` is typed as `readonly ArtifactInput[]`.
    - `ProviderRunResponse.artifactRefs` is typed as `readonly (ArtifactInput | ArtifactRef)[]`.
    - `createAI(...).run(...)` maps `response.artifactRefs` through `toArtifactRef` so `RunSuccess.artifacts` never contains `value`.
    - `output.artifacts({ artifactKind: "document" })` validates that raw artifact output items are artifact refs of kind `document` and strips payloads from valid items.
    - Runtime tests cover a transcript artifact (`transform.kind: "transcription"`), a tool-result artifact (`artifact.toolResult`), and a provider-handle placeholder artifact (`transform.kind: "provider-packaging"`, source `provider-upload`) as artifact refs, not ad hoc fields.
  </behavior>
  <action>
    In `packages/lattice/src/providers/provider.ts`, import `ArtifactInput` and `ArtifactRef` from the artifact module. Change `ProviderRunRequest.artifacts` from `readonly unknown[]` to `readonly ArtifactInput[]`. Change `ProviderRunResponse.artifactRefs` from `readonly unknown[]` to `readonly (ArtifactInput | ArtifactRef)[]`.

    In `packages/lattice/src/runtime/create-ai.ts`, import `toArtifactRef` and remove the current `as readonly ArtifactRef[]` cast. When `response.artifactRefs` exists, return `response.artifactRefs.map(toArtifactRef)` for `RunSuccess.artifacts`; otherwise return `[]`. This implements ART-05 by making generated artifacts payload-free refs at the run boundary.

    In `packages/lattice/src/outputs/validate.ts`, import `isArtifactRef` and `toArtifactRef`. For the `ArtifactRefsOutputContract` branch, keep the existing array check, then validate every item with `isArtifactRef`. If `contract.artifactKind` is set, reject any item whose `kind` does not equal it with issue message `Expected artifacts output item kind to be "${contract.artifactKind}".` Return `value.map(toArtifactRef)` so named artifact outputs are also payload-free.

    Update `packages/lattice/test/outputs.test.ts` to cover artifact output validation success, payload stripping, invalid item rejection, and artifactKind mismatch rejection. Update `packages/lattice/test/runtime.test.ts` so the fixture adapter returns artifact refs created with `artifact.derive(...)` and `artifact.toolResult(...)`, and assert `result.artifacts` contains lineage/source/metadata but no `value`. Represent provider handles only as source/lineage metadata; do not implement uploads or provider message parts per D-21.
  </action>
  <verify>
    <automated>pnpm --filter lattice test -- outputs.test.ts runtime.test.ts && pnpm --filter lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'readonly ArtifactInput\\[\\]|readonly \\(ArtifactInput \\| ArtifactRef\\)\\[\\]' packages/lattice/src/providers/provider.ts` finds typed provider artifact boundaries.
    - `rg 'toArtifactRef' packages/lattice/src/runtime/create-ai.ts packages/lattice/src/outputs/validate.ts` finds runtime and output payload stripping.
    - `rg 'isArtifactRef|artifactKind' packages/lattice/src/outputs/validate.ts packages/lattice/test/outputs.test.ts` finds artifact output validation.
    - `rg 'transcription|provider-packaging|toolResult' packages/lattice/test/runtime.test.ts packages/lattice/test/outputs.test.ts` finds generated/tool/transcript/provider-handle artifact scenarios.
    - `pnpm --filter lattice test -- outputs.test.ts runtime.test.ts` passes.
  </acceptance_criteria>
  <done>Runtime and output boundaries carry generated/tool/transcript/provider-handle results as payload-free artifact refs with metadata and lineage, not ad hoc raw fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export artifact lifecycle and storage APIs through lattice</name>
  <read_first>
    .planning/phases/02-artifact-lifecycle-storage/02-CONTEXT.md
    .planning/phases/02-artifact-lifecycle-storage/02-RESEARCH.md
    .planning/phases/02-artifact-lifecycle-storage/02-01-SUMMARY.md
    .planning/phases/02-artifact-lifecycle-storage/02-02-SUMMARY.md
    packages/lattice/src/artifacts/artifact.ts
    packages/lattice/src/artifacts/lineage.ts
    packages/lattice/src/storage/storage.ts
    packages/lattice/src/storage/memory.ts
    packages/lattice/src/storage/local.ts
    packages/lattice/src/runtime/public-types.ts
    packages/lattice/src/index.ts
    packages/lattice/test/public-api.test-d.ts
    packages/lattice/test-d/package-types.test-d.ts
  </read_first>
  <files>packages/lattice/src/runtime/public-types.ts, packages/lattice/src/index.ts, packages/lattice/test/public-api.test-d.ts, packages/lattice/test-d/package-types.test-d.ts</files>
  <behavior>
    - Source and built package type tests can import `artifact`, `createMemoryArtifactStore`, `createLocalArtifactStore`, `ArtifactRef`, `ArtifactInput`, `ArtifactPrivacy`, `ArtifactSize`, `ArtifactFingerprint`, `ArtifactStorageRef`, `ArtifactLineage`, `ArtifactTransformDescriptor`, `ArtifactStore`, `StorageLike`, and `StoredArtifactEnvelope` from the public entrypoint.
    - Public type tests can construct text, JSON, image, audio, document, URL, tool-result, and derived transcript artifacts through `artifact`.
    - Public type tests can call `await createMemoryArtifactStore().put(...)`, `get(...)`, and `load(...)` and observe `ArtifactRef | undefined` versus `ArtifactInput | undefined` types.
    - Package-boundary `tsd` tests compile against built declarations after `pnpm --filter lattice build`.
  </behavior>
  <action>
    In `packages/lattice/src/index.ts`, add named value exports:

    ```typescript
    export { createMemoryArtifactStore } from "./storage/memory.js";
    export { createLocalArtifactStore } from "./storage/local.js";
    ```

    Keep `artifact`, `output`, and `createAI` named exports unchanged per Phase 1 style and D-01. Do not add default exports.

    In `packages/lattice/src/runtime/public-types.ts`, export all public artifact lifecycle types from `../artifacts/artifact.js` and `../artifacts/lineage.js`, including `ArtifactInput`, `ArtifactKind`, `ArtifactRef`, `ArtifactSource`, `ArtifactOptions`, `ArtifactPrivacy`, `ArtifactSize`, `ArtifactFingerprint`, `ArtifactStorageRef`, `ArtifactLineage`, `ArtifactParentRef`, `ArtifactTransformDescriptor`, and `ArtifactTransformKind`. Export `ArtifactStore`, `StorageLike`, and `StoredArtifactEnvelope` from `../storage/storage.js`.

    Update `packages/lattice/test/public-api.test-d.ts` and `packages/lattice/test-d/package-types.test-d.ts` to import from the public entrypoint and assert source/package types for constructor coverage, lineage, memory store get/load separation, local store factory typing, and generated artifact outputs. In the package `tsd` test, include an async function that constructs `artifact.document("manual.pdf")`, `artifact.toolResult(...)`, a derived transcript with `transform.kind: "transcription"`, and `await createMemoryArtifactStore().put(...)`.
  </action>
  <verify>
    <automated>pnpm --filter lattice build && pnpm --filter lattice test:types && pnpm --filter lattice lint:packages</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'createMemoryArtifactStore|createLocalArtifactStore' packages/lattice/src/index.ts packages/lattice/test-d/package-types.test-d.ts` finds public value exports and package tests.
    - `rg 'ArtifactPrivacy|ArtifactSize|ArtifactFingerprint|ArtifactStorageRef|ArtifactLineage|ArtifactTransformDescriptor|ArtifactStore|StoredArtifactEnvelope' packages/lattice/src/runtime/public-types.ts packages/lattice/test/public-api.test-d.ts packages/lattice/test-d/package-types.test-d.ts` finds public type exports and tests.
    - `rg 'export default' packages/lattice/src packages/lattice/test packages/lattice/test-d` returns no matches.
    - `pnpm --filter lattice build && pnpm --filter lattice test:types` passes.
    - `pnpm --filter lattice lint:packages` passes with the existing accepted ESM-only profile behavior.
  </acceptance_criteria>
  <done>The public `lattice` entrypoint exposes the artifact lifecycle and development store APIs with source and built package type coverage.</done>
</task>

</tasks>

<verification>
Run the full Phase 2 package checks:

```bash
pnpm --filter lattice test
pnpm --filter lattice typecheck
pnpm --filter lattice build
pnpm --filter lattice test:types
pnpm --filter lattice lint:packages
rg 'from "openai"|from "ai"|@ai-sdk|anthropic|gemini' packages/lattice/src packages/lattice/test packages/lattice/test-d
rg 'sqlite|postgres|s3|aws-sdk|sharp|ffmpeg|mcp' packages/lattice/src packages/lattice/test packages/lattice/test-d
```

The provider SDK leak scan and deferred dependency scan must return no matches.
</verification>

<success_criteria>
All Phase 2 requirements are covered through the public API: constructors for required artifact kinds, stable metadata/ref fields, lineage descriptors, memory/local stores, and generated/tool/transcript/provider-handle outputs represented as artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-artifact-lifecycle-storage/02-03-SUMMARY.md`.
</output>
