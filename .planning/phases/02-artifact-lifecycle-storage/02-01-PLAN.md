---
phase: 02-artifact-lifecycle-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
  - packages/lattice/package.json
  - packages/lattice/src/artifacts/artifact.ts
  - packages/lattice/src/artifacts/metadata.ts
  - packages/lattice/src/artifacts/lineage.ts
  - packages/lattice/test/artifacts.test.ts
autonomous: true
requirements: [ART-01, ART-02, ART-03, ART-05]
requirements_addressed: [ART-01, ART-02, ART-03, ART-05]
must_haves:
  truths:
    - "Developer can synchronously construct text, JSON, file, image, audio, document, URL, and tool-result artifacts through the named artifact namespace."
    - "Every constructed artifact has an opaque stable id, kind, source, privacy label, optional media type, optional caller metadata, optional storage ref, optional fingerprint, and cheap size metadata when available."
    - "Developer can create derived/generated artifacts that preserve parent refs and a transform descriptor for extraction, chunking, transcription, resizing, provider packaging, tool results, and model outputs."
  artifacts:
    - path: "packages/lattice/src/artifacts/artifact.ts"
      provides: "Public artifact namespace, ArtifactRef/Input types, toArtifactRef/isArtifactRef helpers, and synchronous constructors"
      contains: "toolResult"
    - path: "packages/lattice/src/artifacts/metadata.ts"
      provides: "Cheap media type and size metadata helpers"
      contains: "inferMediaType"
    - path: "packages/lattice/src/artifacts/lineage.ts"
      provides: "Lineage and transform descriptor types"
      contains: "provider-packaging"
    - path: "packages/lattice/test/artifacts.test.ts"
      provides: "Runtime coverage for artifact helper construction, metadata defaults, ids, privacy, and lineage"
  key_links:
    - from: "packages/lattice/src/artifacts/artifact.ts"
      to: "packages/lattice/src/policy/policy.ts"
      via: "ArtifactPrivacy uses the PolicySpec privacy vocabulary"
      pattern: "NonNullable<PolicySpec\\[\"privacy\"\\]>"
    - from: "packages/lattice/src/artifacts/artifact.ts"
      to: "packages/lattice/src/artifacts/metadata.ts"
      via: "constructors call cheap metadata/media inference helpers"
      pattern: "inferMediaType|measureArtifactValue"
    - from: "packages/lattice/src/artifacts/artifact.ts"
      to: "packages/lattice/src/artifacts/lineage.ts"
      via: "derived artifacts attach parent refs and transform descriptors"
      pattern: "ArtifactLineage|ArtifactTransformDescriptor"
---

<objective>
Expand the Phase 1 artifact stubs into the provider-neutral artifact data contract.

Purpose: Phase 2 needs reusable artifact records before storage, runtime output refs, or later provider packaging can rely on artifact metadata and lineage.
Output: Synchronous artifact constructors, cheap metadata inference, privacy defaults, opaque ids, payload-free ref helpers, and explicit lineage descriptors.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-artifact-lifecycle-storage/02-CONTEXT.md
@.planning/phases/02-artifact-lifecycle-storage/02-RESEARCH.md
@.planning/phases/01-runtime-api-output-contracts/01-CONTEXT.md
@.planning/phases/01-runtime-api-output-contracts/01-VERIFICATION.md
@packages/lattice/src/artifacts/artifact.ts
@packages/lattice/src/policy/policy.ts
@packages/lattice/test/runtime-config.test.ts

<interfaces>
Existing source contracts to preserve:

```typescript
export type ArtifactKind =
  | "text"
  | "json"
  | "file"
  | "image"
  | "audio"
  | "video"
  | "document"
  | "url"
  | "tool-result";

export interface ArtifactRef {
  readonly id: string;
  readonly kind: ArtifactKind;
  readonly mediaType?: string;
  readonly source: ArtifactSource;
  readonly label?: string;
  readonly metadata?: Record<string, unknown>;
}

export type ArtifactInput = ArtifactRef & {
  readonly value?: unknown;
};
```

Locked decisions implemented here: D-01 through D-12 and D-18 through D-21 from `02-CONTEXT.md`. Deferred items that must not be implemented: file IO, real media parsing, uploads, provider packaging execution, extraction/chunking/transcription/resizing implementations, S3, SQLite, Postgres, and tool execution.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand artifact constructors and cheap metadata defaults</name>
  <read_first>
    .planning/phases/02-artifact-lifecycle-storage/02-CONTEXT.md
    .planning/phases/02-artifact-lifecycle-storage/02-RESEARCH.md
    packages/lattice/src/artifacts/artifact.ts
    packages/lattice/src/policy/policy.ts
    packages/lattice/package.json
    pnpm-workspace.yaml
  </read_first>
  <files>pnpm-workspace.yaml, pnpm-lock.yaml, packages/lattice/package.json, packages/lattice/src/artifacts/artifact.ts, packages/lattice/src/artifacts/metadata.ts, packages/lattice/test/artifacts.test.ts</files>
  <behavior>
    - `artifact.text("hello", { privacy: "sensitive", id: "artifact:text:test" })` returns kind `text`, source `inline`, media type `text/plain`, privacy `sensitive`, value `"hello"`, id override unchanged, size `{ characters: 5, bytes: 5 }`.
    - `artifact.json({ ok: true })` returns kind `json`, source `inline`, media type `application/json`, privacy `standard`, and byte/character size based on `JSON.stringify`.
    - `artifact.file("invoice.pdf")`, `artifact.image("photo.jpg")`, `artifact.audio("call.mp3")`, and `artifact.document("manual.pdf")` do not read the path and infer media types from path/extension when obvious.
    - `artifact.url(new URL("https://example.test/a"))` returns kind `url`, source `url`, and value `"https://example.test/a"`.
    - `artifact.toolResult({ status: "ok" }, { toolName: "lookup", callId: "call-1" })` returns kind `tool-result`, source `tool`, media type `application/json`, and metadata containing `toolName: "lookup"` and `callId: "call-1"`.
  </behavior>
  <action>
    Write tests first in `packages/lattice/test/artifacts.test.ts` for the constructor behaviors above, then implement them. Add `mime: 4.1.0` to the root `pnpm-workspace.yaml` catalog and add `"mime": "catalog:"` to `packages/lattice/package.json` dependencies; update `pnpm-lock.yaml` by running `pnpm install --lockfile-only`.

    In `packages/lattice/src/artifacts/artifact.ts`, keep the named `artifact` namespace per D-01 and extend it with exactly these synchronous constructor methods per D-02 and D-03: `text`, `json`, `file`, `image`, `audio`, `document`, `url`, and `toolResult`. Do not add video-specific helpers or behavior per D-04. Keep all helpers synchronous and free of file IO, hashing, uploads, or payload movement per D-05.

    Add `ArtifactPrivacy` as `NonNullable<PolicySpec["privacy"]>` so privacy uses the Phase 1 policy vocabulary per D-09. Add `ArtifactSize` with optional `bytes`, `characters`, `pages`, `width`, `height`, and `durationMs`; `ArtifactFingerprint` with `algorithm: "sha256"` and `value`; and `ArtifactStorageRef` with `storeId` and `key` per D-08 through D-11. Extend `ArtifactOptions` and `ArtifactRef` with `privacy`, `size`, `fingerprint`, and `storage`, defaulting `privacy` to `"standard"` per D-09.

    Create `packages/lattice/src/artifacts/metadata.ts` with `inferMediaType(value, options)` and `measureArtifactValue(value, kind)` helpers. Use `mime.getType(path)` for string path inference per research and D-12. Use Blob/File `type` and `size` when present. Measure text and JSON with `TextEncoder`; do not parse PDF/audio/image bytes or stat paths.
  </action>
  <verify>
    <automated>pnpm install --lockfile-only && pnpm --filter lattice test -- artifacts.test.ts && pnpm --filter lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg '"mime": "catalog:"' packages/lattice/package.json` finds the package dependency.
    - `rg 'text\\(|json\\(|file\\(|image\\(|audio\\(|document\\(|url\\(|toolResult\\(' packages/lattice/src/artifacts/artifact.ts` finds all required artifact namespace helpers.
    - `rg 'export type ArtifactPrivacy|export interface ArtifactSize|export interface ArtifactFingerprint|export interface ArtifactStorageRef' packages/lattice/src/artifacts/artifact.ts packages/lattice/src/artifacts/metadata.ts` finds the metadata/privacy/storage/fingerprint types.
    - `rg 'privacy: options\\.privacy \\?\\? "standard"' packages/lattice/src/artifacts/artifact.ts` finds the default privacy assignment.
    - `! rg 'readFile|stat\\(|writeFile|createHash|upload' packages/lattice/src/artifacts` returns no matches.
    - `pnpm --filter lattice test -- artifacts.test.ts` passes.
  </acceptance_criteria>
  <done>All required artifact constructors exist, produce stable opaque ids or caller-provided ids, attach cheap metadata and privacy defaults, and do not perform IO or hashing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add lineage, derived artifacts, and payload-free refs</name>
  <read_first>
    .planning/phases/02-artifact-lifecycle-storage/02-CONTEXT.md
    packages/lattice/src/artifacts/artifact.ts
    packages/lattice/src/artifacts/metadata.ts
    packages/lattice/test/artifacts.test.ts
  </read_first>
  <files>packages/lattice/src/artifacts/artifact.ts, packages/lattice/src/artifacts/lineage.ts, packages/lattice/test/artifacts.test.ts</files>
  <behavior>
    - `artifact.derive({ kind: "text", value: "transcript", source: "generated", parents: [audioRef], transform: { kind: "transcription", name: "fixture transcript" } })` returns a generated text artifact with lineage parent ref copied from `audioRef`.
    - Lineage transform kinds are type-safe for `manual`, `generated`, `extraction`, `chunking`, `transcription`, `resizing`, `provider-packaging`, `tool-result`, and `model-output`.
    - `toArtifactRef(artifact.text("secret"))` returns a payload-free object with no `value` property.
    - `isArtifactRef({ id: "artifact:text:1", kind: "text", source: "generated", privacy: "standard" })` returns true.
  </behavior>
  <action>
    Create `packages/lattice/src/artifacts/lineage.ts` with `ArtifactTransformKind`, `ArtifactTransformDescriptor`, `ArtifactParentRef`, and `ArtifactLineage`. `ArtifactTransformKind` must include the exact literals `manual`, `generated`, `extraction`, `chunking`, `transcription`, `resizing`, `provider-packaging`, `tool-result`, and `model-output` per D-18 through D-21. `ArtifactLineage` must contain `parents: readonly ArtifactParentRef[]` and `transform: ArtifactTransformDescriptor`.

    In `artifact.ts`, add `lineage?: ArtifactLineage` to `ArtifactRef` and `ArtifactOptions`. Add `ArtifactDerivedOptions` for `artifact.derive(input)` where `input` has exact fields `kind`, optional `source`, optional `value`, `parents`, and `transform`, plus the existing artifact options. `artifact.derive` must convert parents with `toArtifactRef` and store only parent ref fields in lineage, not raw parent payloads.

    Add `toArtifactRef(artifact)` and `isArtifactRef(value)` exports. `toArtifactRef` must return id, kind, source, privacy, and optional mediaType, label, metadata, size, fingerprint, storage, and lineage, but must not include `value`. Use `toArtifactRef` internally for lineage parent refs. Keep provider packaging as a descriptor only; do not implement provider uploads or message-part packaging per deferred scope.
  </action>
  <verify>
    <automated>pnpm --filter lattice test -- artifacts.test.ts && pnpm --filter lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'export type ArtifactTransformKind' packages/lattice/src/artifacts/lineage.ts` finds the transform union.
    - `rg 'provider-packaging|transcription|model-output|tool-result' packages/lattice/src/artifacts/lineage.ts packages/lattice/test/artifacts.test.ts` finds descriptor coverage.
    - `rg 'derive\\(|toArtifactRef|isArtifactRef' packages/lattice/src/artifacts/artifact.ts packages/lattice/test/artifacts.test.ts` finds the helper and tests.
    - `rg 'value' packages/lattice/test/artifacts.test.ts` includes an assertion that `toArtifactRef(...)` omits payload `value`.
    - `pnpm --filter lattice test -- artifacts.test.ts` passes.
  </acceptance_criteria>
  <done>Derived/generated artifacts can preserve parent lineage and transform descriptors, while public refs stay payload-free.</done>
</task>

</tasks>

<verification>
Run the plan-level checks after both tasks:

```bash
pnpm --filter lattice test -- artifacts.test.ts
pnpm --filter lattice typecheck
rg 'from "openai"|from "ai"|@ai-sdk|anthropic|gemini' packages/lattice/src packages/lattice/test
```

The provider SDK leak scan must return no matches.
</verification>

<success_criteria>
ART-01 is satisfied for artifact constructors, ART-02 is satisfied for artifact identity/metadata/privacy/storage/fingerprint fields, ART-03 is satisfied for lineage descriptors, and ART-05 has the artifact representation foundation for generated/tool/transcript/provider-handle refs without implementing deferred provider/tool execution.
</success_criteria>

<output>
After completion, create `.planning/phases/02-artifact-lifecycle-storage/02-01-SUMMARY.md`.
</output>
