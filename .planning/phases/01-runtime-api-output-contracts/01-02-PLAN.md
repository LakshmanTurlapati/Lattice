---
phase: 01-runtime-api-output-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - packages/lattice/src/artifacts/artifact.ts
  - packages/lattice/src/providers/provider.ts
  - packages/lattice/src/policy/policy.ts
  - packages/lattice/src/storage/storage.ts
  - packages/lattice/src/tracing/tracing.ts
  - packages/lattice/src/sessions/session.ts
  - packages/lattice/src/plan/plan.ts
  - packages/lattice/src/runtime/config.ts
  - packages/lattice/test/runtime-config.test.ts
autonomous: true
requirements:
  - API-02
must_haves:
  truths:
    - "Developer can call `createAI(config)` with typed providers, storage, defaults, and tracing options."
    - "Provider, storage, tracing, session, and artifact surfaces are Lattice-owned interfaces, not provider SDK types."
    - "Runtime defaults can carry a typed policy skeleton for later use by `ai.run`."
  artifacts:
    - path: "packages/lattice/src/runtime/config.ts"
      provides: "LatticeConfig and normalizeConfig"
      contains: "export interface LatticeConfig"
    - path: "packages/lattice/src/providers/provider.ts"
      provides: "ProviderRef and ProviderAdapter opaque public boundary"
      contains: "export interface ProviderAdapter"
    - path: "packages/lattice/src/artifacts/artifact.ts"
      provides: "Phase 1 artifact helper stubs"
      contains: "export const artifact"
  key_links:
    - from: "packages/lattice/src/runtime/config.ts"
      to: "packages/lattice/src/providers/provider.ts"
      via: "ProviderRegistryInput import"
      pattern: "ProviderRegistryInput"
    - from: "packages/lattice/src/runtime/config.ts"
      to: "packages/lattice/src/policy/policy.ts"
      via: "defaults.policy typing"
      pattern: "policy\\?: PolicySpec"
---

<objective>
Define the typed runtime config and Phase 1 domain stubs that `createAI(config)` will expose.

Purpose: Satisfy API-02 without implementing real providers, storage, tracing, sessions, or artifact lifecycle. Later plans wire these contracts into output validation and the public runtime.
Output: Lattice-owned provider, policy, storage, tracing, session, plan, artifact, and config modules with tests for config normalization.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-runtime-api-output-contracts/01-CONTEXT.md
@.planning/phases/01-runtime-api-output-contracts/01-RESEARCH.md
@.planning/phases/01-runtime-api-output-contracts/01-01-SUMMARY.md
@AGENTS.md

<interfaces>
Created by Plan 01:
```typescript
export const latticeVersion = "0.0.0";
```

Locked decisions implemented here:
- D-03: Expose public `artifact.*` helper types and stubs; real artifact lifecycle/storage is Phase 2.
- D-04: Expose session as a placeholder shape only.
- D-05: `createAI(config)` includes typed `providers`, `storage`, `defaults`, and `tracing`.
- D-06: Provider configuration uses opaque `ProviderRef` / `ProviderAdapter` shapes.
- D-07: Policy supports runtime defaults through `createAI({ defaults: { policy } })`.
- D-08: Storage and tracing are interface-only through `StorageLike` and `TracerLike`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add provider, policy, storage, tracing, session, and plan contracts</name>
  <files>packages/lattice/src/providers/provider.ts, packages/lattice/src/policy/policy.ts, packages/lattice/src/storage/storage.ts, packages/lattice/src/tracing/tracing.ts, packages/lattice/src/sessions/session.ts, packages/lattice/src/plan/plan.ts</files>
  <read_first>
    - packages/lattice/src/providers/provider.ts
    - packages/lattice/src/policy/policy.ts
    - packages/lattice/src/storage/storage.ts
    - packages/lattice/src/tracing/tracing.ts
    - packages/lattice/src/sessions/session.ts
    - packages/lattice/src/plan/plan.ts
    - packages/lattice/tsconfig.json
    - .planning/phases/01-runtime-api-output-contracts/01-CONTEXT.md
    - .planning/phases/01-runtime-api-output-contracts/01-RESEARCH.md
  </read_first>
  <behavior>
    - Type test: a `ProviderRef` can be `{ id: "fixture", kind: "provider-ref" }`.
    - Type test: a `ProviderAdapter` can expose optional `execute(request)` returning `{ rawOutputs }`.
    - Runtime test: a policy object can include `maxCostUsd`, `latency`, `privacy`, `noUpload`, `noPublicUrl`, and `noLogging`.
  </behavior>
  <action>
    Create these exact exported contracts.

    In `providers/provider.ts`:
    - `ProviderRef` with `readonly id: string` and `readonly kind?: "provider-ref"`.
    - `ProviderRunRequest` with `readonly task: string`, `readonly artifacts: readonly unknown[]`, `readonly outputs: readonly string[]`, `readonly policy?: unknown`, and `readonly signal?: AbortSignal`.
    - `ProviderRunResponse` with `readonly rawOutputs: Record<string, unknown>` and `readonly artifactRefs?: readonly unknown[]`.
    - `ProviderAdapter` with `readonly id: string`, `readonly kind: "provider-adapter"`, and optional `execute?: (request: ProviderRunRequest) => Promise<ProviderRunResponse>`.
    - `ProviderRegistryInput = readonly (ProviderRef | ProviderAdapter | string)[]`.

    In `policy/policy.ts`:
    - `PolicySpec` with optional readonly fields: `maxCostUsd?: number`, `latency?: "interactive" | "batch"`, `privacy?: "standard" | "sensitive" | "restricted"`, `providerAllowList?: readonly string[]`, `providerDenyList?: readonly string[]`, `noUpload?: boolean`, `noPublicUrl?: boolean`, `noLogging?: boolean`, and `metadata?: Record<string, unknown>`.
    - `mergePolicy(defaultPolicy?: PolicySpec, runPolicy?: PolicySpec): PolicySpec | undefined` that shallow-merges defaults first and run policy second, returning `undefined` if both inputs are absent.

    In `storage/storage.ts`:
    - `StorageLike` with `readonly kind: "storage"`, optional `put?(key: string, value: unknown): Promise<void>`, and optional `get?(key: string): Promise<unknown>`.

    In `tracing/tracing.ts`:
    - `TracerLike` with `readonly kind: "tracer"` and optional `span?<T>(name: string, fn: () => T | Promise<T>): T | Promise<T>`.

    In `sessions/session.ts`:
    - `SessionRef` with `readonly id: string` and `readonly kind?: "session-ref"`.

    In `plan/plan.ts`:
    - `ExecutionPlanStub` with `readonly id: string`, `readonly kind: "plan-stub"`, `readonly createdAt: string`, `readonly status: "stub"`, `readonly stages: readonly []`, and `readonly warnings: readonly string[]`.
    - `createExecutionPlanStub(warnings?: readonly string[]): ExecutionPlanStub` that uses `crypto.randomUUID()` when available and returns `status: "stub"`.

    Keep these contracts provider-neutral. Do not import provider SDK types or implement routing, storage persistence, tracing spans, sessions, or replay behavior.
  </action>
  <verify>
    <automated>pnpm --filter lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/lattice/src/providers/provider.ts` contains `export interface ProviderRef`.
    - `packages/lattice/src/providers/provider.ts` contains `export interface ProviderAdapter`.
    - `packages/lattice/src/providers/provider.ts` contains `execute?: (request: ProviderRunRequest) => Promise<ProviderRunResponse>`.
    - `packages/lattice/src/policy/policy.ts` contains `export interface PolicySpec`.
    - `packages/lattice/src/policy/policy.ts` contains `latency?: "interactive" | "batch"`.
    - `packages/lattice/src/storage/storage.ts` contains `export interface StorageLike`.
    - `packages/lattice/src/tracing/tracing.ts` contains `export interface TracerLike`.
    - `packages/lattice/src/sessions/session.ts` contains `export interface SessionRef`.
    - `packages/lattice/src/plan/plan.ts` contains `export interface ExecutionPlanStub`.
    - `rg 'from "openai"|from "ai"|@ai-sdk|anthropic|gemini' packages/lattice/src` returns no matches.
    - `pnpm --filter lattice typecheck` exits 0.
  </acceptance_criteria>
  <done>Lattice-owned config dependency contracts exist without provider SDK leakage or real later-phase behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 1 artifact helpers and config normalization</name>
  <files>packages/lattice/src/artifacts/artifact.ts, packages/lattice/src/runtime/config.ts, packages/lattice/test/runtime-config.test.ts</files>
  <read_first>
    - packages/lattice/src/artifacts/artifact.ts
    - packages/lattice/src/runtime/config.ts
    - packages/lattice/test/runtime-config.test.ts
    - packages/lattice/src/providers/provider.ts
    - packages/lattice/src/policy/policy.ts
    - packages/lattice/src/storage/storage.ts
    - packages/lattice/src/tracing/tracing.ts
    - packages/lattice/src/sessions/session.ts
    - packages/lattice/src/plan/plan.ts
    - .planning/phases/01-runtime-api-output-contracts/01-CONTEXT.md
    - .planning/phases/01-runtime-api-output-contracts/01-RESEARCH.md
  </read_first>
  <behavior>
    - `artifact.text("hello")` returns an object with `kind: "text"`, `source: "inline"`, and `mediaType: "text/plain"`.
    - `artifact.file("invoice.pdf", { mediaType: "application/pdf" })` returns an object with `kind: "file"`, `source: "file"`, and `mediaType: "application/pdf"`.
    - `normalizeConfig({ providers: ["fixture"], defaults: { policy } })` returns one provider ref with id `fixture` and preserves `defaults.policy`.
  </behavior>
  <action>
    Create artifact stubs and config normalization.

    In `artifacts/artifact.ts`:
    - Export `ArtifactKind = "text" | "json" | "file" | "image" | "audio" | "video" | "document" | "url" | "tool-result"`.
    - Export `ArtifactSource = "inline" | "file" | "url" | "generated" | "provider-upload" | "tool"`.
    - Export `ArtifactRef` with readonly `id`, `kind`, optional `mediaType`, `source`, optional `label`, and optional `metadata`.
    - Export `ArtifactInput = ArtifactRef & { readonly value?: unknown }`.
    - Export `artifact` object with helper methods:
      - `text(value: string, options?: ArtifactOptions): ArtifactInput` returns `kind: "text"`, `source: "inline"`, `mediaType: options.mediaType ?? "text/plain"`, and `value`.
      - `json(value: unknown, options?: ArtifactOptions): ArtifactInput` returns `kind: "json"`, `source: "inline"`, `mediaType: options.mediaType ?? "application/json"`, and `value`.
      - `file(value: Blob | File | string, options?: ArtifactOptions): ArtifactInput` returns `kind: "file"`, `source: "file"`, `mediaType: options.mediaType`, and `value`.
      - `url(value: string | URL, options?: ArtifactOptions): ArtifactInput` returns `kind: "url"`, `source: "url"`, `mediaType: options.mediaType`, and `value: value.toString()`.
    - Export `ArtifactOptions` with readonly optional `id`, `mediaType`, `label`, and `metadata`.
    - Use a local `createArtifactId(kind: ArtifactKind): string` helper that returns `artifact:${kind}:${crypto.randomUUID()}` if `crypto.randomUUID` exists and `artifact:${kind}:${Date.now()}:${Math.random().toString(16).slice(2)}` otherwise.
    - Do not read files, hash content, store bytes, sniff MIME types, upload data, or create lineage in Phase 1.

    In `runtime/config.ts`:
    - Export `LatticeConfig` with readonly optional `providers?: ProviderRegistryInput`, `storage?: StorageLike | false`, `defaults?: { readonly policy?: PolicySpec }`, and `tracing?: TracerLike | false`.
    - Export `NormalizedProviderEntry = ProviderRef | ProviderAdapter`.
    - Export `NormalizedLatticeConfig` with readonly `providers: readonly NormalizedProviderEntry[]`, optional `storage?: StorageLike`, readonly `defaults: { readonly policy?: PolicySpec }`, and optional `tracing?: TracerLike`.
    - Export `normalizeConfig(config: LatticeConfig = {}): NormalizedLatticeConfig`.
    - Normalize string providers to `{ id: providerName, kind: "provider-ref" }`.
    - Preserve provider refs and provider adapters unchanged except adding no SDK types.
    - Convert `storage: false` and `tracing: false` to absent normalized values.

    In `test/runtime-config.test.ts`, add Vitest tests for artifact helpers, string provider normalization, storage/tracing false normalization, and policy defaults preservation.
  </action>
  <verify>
    <automated>pnpm --filter lattice test -- runtime-config.test.ts && pnpm --filter lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/lattice/src/artifacts/artifact.ts` contains `export const artifact`.
    - `packages/lattice/src/artifacts/artifact.ts` contains `text(value: string`.
    - `packages/lattice/src/artifacts/artifact.ts` contains `file(value: Blob | File | string`.
    - `rg 'readFile|createHash|upload|sniff|lineage' packages/lattice/src/artifacts/artifact.ts` returns no matches.
    - `packages/lattice/src/runtime/config.ts` contains `export interface LatticeConfig`.
    - `packages/lattice/src/runtime/config.ts` contains `providers?: ProviderRegistryInput`.
    - `packages/lattice/src/runtime/config.ts` contains `defaults?: { readonly policy?: PolicySpec }`.
    - `packages/lattice/src/runtime/config.ts` contains `export function normalizeConfig`.
    - `packages/lattice/test/runtime-config.test.ts` contains `artifact.text("hello")`.
    - `packages/lattice/test/runtime-config.test.ts` contains `normalizeConfig({ providers: ["fixture"]`.
    - `pnpm --filter lattice test -- runtime-config.test.ts` exits 0.
    - `pnpm --filter lattice typecheck` exits 0.
  </acceptance_criteria>
  <done>Config and artifact stubs are typed, tested, and explicitly limited to Phase 1 behavior.</done>
</task>

</tasks>

<verification>
Run `pnpm --filter lattice test -- runtime-config.test.ts` and `pnpm --filter lattice typecheck`. Inspect `packages/lattice/src` with `rg 'openai|@ai-sdk|anthropic|gemini' packages/lattice/src` and confirm there are no provider SDK imports.
</verification>

<success_criteria>
Plan 02 is complete when `LatticeConfig`, `normalizeConfig`, `ProviderRef`, `ProviderAdapter`, `PolicySpec`, `StorageLike`, `TracerLike`, `SessionRef`, `ExecutionPlanStub`, and `artifact.*` Phase 1 helpers exist and tests prove the config surface is provider-neutral.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-api-output-contracts/01-02-SUMMARY.md`.
</output>
